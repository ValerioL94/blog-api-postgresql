import Navbar from './Navbar';

const Header = () => {
  return (
    <header className='flex justify-center text-white bg-blue-700 font-semibold border-b-2 border-gray-500 sticky top-0 '>
      <Navbar />
    </header>
  );
};

export default Header;
